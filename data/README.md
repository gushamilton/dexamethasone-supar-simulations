# Data policy

This repository uses published or approved summary parameters and synthetic
trial data for simulation.

Do not add the historical participant-level dataset, identifiers, dates of
diagnosis, clinical records or credentials to the GitHub repository. If local
validation against participant-level data is required, place source files in
`data/raw/`, which is excluded by `.gitignore`.
